const state={currentDate:new Date(2026,2,24),selectedDate:'2026-03-24',activeFilter:'all',rubrics:['Експертний','Продаючий','Кейс','Лайфстайл','Відгук'],placements:['Instagram Post','Instagram Story','Facebook','TikTok','Telegram'],formats:['Статичний пост','Карусель','Reels','Story','Текстовий пост'],topics:['5 помилок, які вбивають охоплення','Як підготувати контент-план на місяць','Що постити, коли немає ідей','Кейс клієнта: до/після','Як оформити продаючий Reels','Чому рубрика важливіша за тренди'],meetings:[{title:'План контенту на квітень',date:'2026-03-20',notes:'Акцент на прогрів до запуску. Потрібні кейси, behind the scenes, серія корисних постів і контент про біль клієнта.'}],holidays:{'2026-01-01':'Новий рік — підбірка підсумків, плани, акції.','2026-02-14':'День закоханих — романтичні офери, подарунки, емоційний контент.','2026-03-08':'8 березня — тематичні підбірки, подарункові рішення, турбота.','2026-04-12':'Великдень — святкові пропозиції, підбірки, візуали з пасхальним настроєм.','2026-05-10':'День матері — емоційні історії, добірки, спецпропозиції.','2026-06-01':'День захисту дітей — сімейний та дитячий контент.','2026-08-24':'День Незалежності — українські сенси, патріотичний контент.','2026-11-27':'Black Friday — акції, дедлайни, офери, каруселі з пропозиціями.','2026-12-19':'Святий Миколай — подарункові добірки, теплий контент.','2026-12-31':'Передноворічний контент — підсумки, акції, плани на новий рік.'},posts:{'2026-03-04':[{'topic':'Кейс клієнта: до/після','placement':'Instagram Post','rubric':'Кейс','format':'Статичний пост','status':'done'}],'2026-03-07':[{'topic':'Як підготувати контент-план на місяць','placement':'Telegram','rubric':'Експертний','format':'Текстовий пост','status':'draft'}],'2026-03-12':[{'topic':'Що постити, коли немає ідей','placement':'Instagram Story','rubric':'Лайфстайл','format':'Story','status':'gen'}],'2026-03-24':[{'topic':'5 помилок, які вбивають охоплення','placement':'Instagram Post','rubric':'Експертний','format':'Карусель','status':'draft'}],'2026-04-02':[{'topic':'Як оформити продаючий Reels','placement':'TikTok','rubric':'Продаючий','format':'Reels','status':'done'}]}};
const monthNames=['Січень','Лютий','Березень','Квітень','Травень','Червень','Липень','Серпень','Вересень','Жовтень','Листопад','Грудень'];const monthNamesGen=['січня','лютого','березня','квітня','травня','червня','липня','серпня','вересня','жовтня','листопада','грудня'];const placementSizes={'Instagram Post':'1080×1350','Instagram Story':'1080×1920','Facebook':'1200×1500','TikTok':'1080×1920','Telegram':'1280×1280'};
const pad=n=>String(n).padStart(2,'0'); const dateKey=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`; const humanDate=str=>{const d=new Date(str); return `${d.getDate()} ${monthNamesGen[d.getMonth()]} ${d.getFullYear()}`}; const statusLabel=s=>s==='done'?'Затверджено':s==='gen'?'Згенеровано':'Чернетка'; const getHolidayHint=d=>state.holidays[d]||'Немає рекомендації'; const countHolidayMatches=(y,m)=>Object.keys(state.holidays).filter(k=>{const d=new Date(k);return d.getFullYear()===y&&d.getMonth()===m}).length; const countAllPosts=()=>Object.values(state.posts).reduce((a,b)=>a+b.length,0); const countDrafts=()=>Object.values(state.posts).flat().filter(p=>p.status==='draft').length;
function saveState(){localStorage.setItem('aima-smm-mvp',JSON.stringify(state))} function loadState(){const s=localStorage.getItem('aima-smm-mvp'); if(!s)return; try{const p=JSON.parse(s); Object.assign(state,p); state.currentDate=new Date(p.currentDate)}catch{}}
function navigate(p){if(p==='calendar')location.href='./index.html'; if(p==='settings')location.href='./settings.html'; if(p==='topics')location.href='./topics.html'; if(p==='meetings')location.href='./meetings.html'}
function bindNav(){document.querySelectorAll('[data-nav]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.nav)))}
function enableEnterSubmit(formId,handler){const f=document.getElementById(formId); if(!f)return; f.addEventListener('submit',e=>{e.preventDefault(); handler();})}
function renderSidebar(active){const menu=document.getElementById('sidebarMenu'); if(!menu)return; const items=[['calendar','Контент-календар'],['topics','Теми'],['meetings','Наради'],['settings','Константи']]; menu.innerHTML=items.map(([k,l])=>`<button class="side-link ${active===k?'active':''}" data-nav="${k}">${l}</button>`).join(''); bindNav()}
function renderHeader(title,subtitle='Aima • SMM Planner MVP'){const t=document.getElementById('pageTitle'); const s=document.getElementById('pageSubtitle'); if(t)t.textContent=title; if(s)s.textContent=subtitle}
function fillSelect(id,items){const el=document.getElementById(id); if(el)el.innerHTML=items.map(i=>`<option value="${i}">${i}</option>`).join('')}
function bindCollection(key,listId,inputId,addId,after=()=>{}){const render=()=>{const list=document.getElementById(listId); if(!list)return; list.innerHTML=state[key].map((item,idx)=>`<div class="setting-row"><span>${item}</span><div class="row-actions"><button class="mini-btn" data-edit="${idx}">Ред.</button><button class="mini-btn danger" data-remove="${idx}">Вид.</button></div></div>`).join(''); list.querySelectorAll('[data-remove]').forEach(btn=>btn.addEventListener('click',()=>{state[key].splice(Number(btn.dataset.remove),1); saveState(); render(); after();})); list.querySelectorAll('[data-edit]').forEach(btn=>btn.addEventListener('click',()=>{const idx=Number(btn.dataset.edit); const next=prompt('Нове значення',state[key][idx]); if(next){state[key][idx]=next; saveState(); render(); after();}}));}; const add=()=>{const input=document.getElementById(inputId); const val=input?.value.trim(); if(!val)return; state[key].push(val); input.value=''; saveState(); render(); after();}; const btn=document.getElementById(addId); if(btn&&!btn.dataset.bound){btn.dataset.bound='1'; btn.addEventListener('click',e=>{if(btn.type!=='submit'){e.preventDefault(); add();}})} enableEnterSubmit(btn?.form?.id,add); render();}
function applyPlacementSize(p){const box=document.getElementById('previewBox'); const size=placementSizes[p]||'1080×1350'; const sizeEl=document.getElementById('visualSize'); if(sizeEl)sizeEl.textContent=`Рекомендований розмір: ${size}`; if(box) box.style.aspectRatio=p.includes('Story')||p==='TikTok'?'9 / 16':p==='Telegram'?'1 / 1':'4 / 5'}
function generateTopicsFromMeetings(){const ideas=[]; state.meetings.forEach(m=>{const text=`${m.title} ${m.notes}`.toLowerCase(); if(text.includes('кейс'))ideas.push('Кейс: як ми отримали результат для клієнта'); if(text.includes('прогрів'))ideas.push('Серія прогрівочних постів перед запуском'); if(text.includes('біль'))ideas.push('3 болі клієнта, які заважають купити зараз'); if(text.includes('вебінар'))ideas.push('Як підготувати аудиторію до вебінару через контент'); if(text.includes('курс'))ideas.push('Що реально дає курс і кому він підходить'); if(text.includes('акці'))ideas.push('Як подати акційну пропозицію без дешевого вигляду'); if(text.includes('закуліс'))ideas.push('Закулісся процесу: як ми готуємо продукт / послугу'); if(text.includes('контент'))ideas.push('Контент-план на тиждень: що постити по днях'); if(text.includes('запуск'))ideas.push('Як підігріти аудиторію перед запуском'); if(text.includes('офер'))ideas.push('Офер, який хочеться зберегти і переслати');}); const unique=[...new Set(ideas)].filter(Boolean); const fallback=['Розбір запиту клієнта: що стоїть за покупкою','Як вибудувати контент, який веде до заявки','Поширені помилки у комунікації бренду','Контент, який прогріває без тиску','Що показувати в контенті, коли продукт уже готовий']; return (unique.length?unique:fallback).slice(0,5)}
function renderCalendarPage(){renderSidebar('calendar'); renderHeader('Контент-календар'); document.getElementById('statPosts').textContent=countAllPosts(); document.getElementById('statTopics').textContent=state.topics.length; document.getElementById('statHolidays').textContent=countHolidayMatches(state.currentDate.getFullYear(),state.currentDate.getMonth()); document.getElementById('statDrafts').textContent=countDrafts(); document.getElementById('weekdays').innerHTML=['Пн','Вт','Ср','Чт','Пт','Сб','Нд'].map(d=>`<div class="weekday">${d}</div>`).join('');
 const currentDraft=()=>{const prev=(state.posts[state.selectedDate]||[])[0]||{}; return {topic:document.getElementById('customTopic').value||document.getElementById('topicPreset').value,rubric:document.getElementById('rubricSelect').value,placement:document.getElementById('placementSelect').value,format:document.getElementById('formatSelect').value,text:document.getElementById('generatedText').value,image:prev.image||null,status:'draft'}};
 const fillEditor=()=>{const post=(state.posts[state.selectedDate]||[])[0]||{topic:'',placement:state.placements[0],rubric:state.rubrics[0],format:state.formats[0],status:'draft',image:null}; document.getElementById('selectedDate').textContent=humanDate(state.selectedDate); document.getElementById('postDate').value=state.selectedDate; document.getElementById('holidayHint').textContent=getHolidayHint(state.selectedDate); fillSelect('topicPreset',state.topics); document.getElementById('customTopic').value=post.topic||''; fillSelect('rubricSelect',state.rubrics); fillSelect('placementSelect',state.placements); fillSelect('formatSelect',state.formats); document.getElementById('rubricSelect').value=post.rubric||state.rubrics[0]; document.getElementById('placementSelect').value=post.placement||state.placements[0]; document.getElementById('formatSelect').value=post.format||state.formats[0]; document.getElementById('generatedText').value=post.text||''; document.getElementById('imageTitle').textContent=post.topic||'Новий контент'; document.getElementById('imageDesc').textContent=`Плейсмент: ${post.placement||state.placements[0]} • Формат: ${post.format||state.formats[0]}`; const badge=document.getElementById('statusBadge'); badge.className=`status-badge ${post.status||'draft'}`; badge.textContent=statusLabel(post.status||'draft'); applyPlacementSize(post.placement||state.placements[0]); const img=document.getElementById('uploadedPreview'); if(post.image){img.src=post.image; img.classList.remove('hidden')} else {img.src=''; img.classList.add('hidden')}};
 const drawFilters=()=>{const el=document.getElementById('filters'); const list=[{id:'all',label:'Всі'},...state.placements.map(p=>({id:p,label:p}))]; el.innerHTML=list.map(i=>`<button class="chip ${state.activeFilter===i.id?'active':''}" data-filter="${i.id}" type="button">${i.label}</button>`).join(''); el.querySelectorAll('button').forEach(btn=>btn.addEventListener('click',()=>{state.activeFilter=btn.dataset.filter; saveState(); drawFilters(); drawCalendar();}))};
 const drawCalendar=()=>{const grid=document.getElementById('calendarGrid'); const y=state.currentDate.getFullYear(),m=state.currentDate.getMonth(); document.getElementById('currentMonth').textContent=`${monthNames[m]} ${y}`; document.getElementById('statHolidays').textContent=countHolidayMatches(y,m); grid.innerHTML=''; const first=new Date(y,m,1); const days=new Date(y,m+1,0).getDate(); const firstDay=(first.getDay()+6)%7; for(let i=0;i<firstDay;i++) grid.innerHTML+='<div class="day empty"></div>'; for(let day=1;day<=days;day++){const date=new Date(y,m,day), key=dateKey(date), posts=state.posts[key]||[], filtered=state.activeFilter==='all'?posts:posts.filter(p=>p.placement===state.activeFilter), selected=key===state.selectedDate?'selected':'', holiday=state.holidays[key]?`<div class="post-pill gen"><strong>🎉</strong><span>${state.holidays[key].slice(0,22)}...</span></div>`:''; grid.innerHTML+=`<div class="day ${selected}" data-date="${key}"><div class="day-head"><span class="day-num">${day}</span><button class="icon-btn add-post" data-date="${key}" type="button">+</button></div><div class="day-posts">${holiday}${filtered.slice(0,2).map(post=>`<div class="post-pill ${post.status||'draft'}"><strong>${post.topic}</strong><span>${post.placement}</span></div>`).join('')}</div></div>`;} grid.querySelectorAll('.day[data-date]').forEach(el=>el.addEventListener('click',e=>{if(e.target.classList.contains('add-post'))return; state.selectedDate=el.dataset.date; saveState(); drawCalendar(); fillEditor();})); grid.querySelectorAll('.add-post').forEach(btn=>btn.addEventListener('click',e=>{e.stopPropagation(); state.selectedDate=btn.dataset.date; saveState(); fillEditor(); drawCalendar(); document.getElementById('customTopic').focus();}))};
 const savePost=(status='draft')=>{const d=currentDraft(); d.status=status; state.posts[state.selectedDate]=[d]; saveState(); fillEditor(); drawCalendar(); document.getElementById('statPosts').textContent=countAllPosts(); document.getElementById('statDrafts').textContent=countDrafts();};
 document.getElementById('prevMonth').addEventListener('click',()=>{state.currentDate=new Date(state.currentDate.getFullYear(),state.currentDate.getMonth()-1,1); saveState(); drawCalendar();}); document.getElementById('nextMonth').addEventListener('click',()=>{state.currentDate=new Date(state.currentDate.getFullYear(),state.currentDate.getMonth()+1,1); saveState(); drawCalendar();}); document.getElementById('usePreset').addEventListener('click',()=>{document.getElementById('customTopic').value=document.getElementById('topicPreset').value}); document.getElementById('placementSelect').addEventListener('change',e=>{applyPlacementSize(e.target.value); document.getElementById('imageDesc').textContent=`Плейсмент: ${e.target.value} • Формат: ${document.getElementById('formatSelect').value}`}); document.getElementById('formatSelect').addEventListener('change',e=>{document.getElementById('imageDesc').textContent=`Плейсмент: ${document.getElementById('placementSelect').value} • Формат: ${e.target.value}`}); document.getElementById('generateText').addEventListener('click',()=>{const topic=document.getElementById('customTopic').value||document.getElementById('topicPreset').value, rubric=document.getElementById('rubricSelect').value, placement=document.getElementById('placementSelect').value, format=document.getElementById('formatSelect').value, holiday=getHolidayHint(state.selectedDate), size=placementSizes[placement]||'1080×1350', meetingsContext=state.meetings.slice(-3).map(m=>`${m.title}: ${m.notes}`).join(' | '), generationMode=document.getElementById('generationMode').value, sourcePost=document.getElementById('sourcePost').value.trim(), mediaContext=[...state.photos.map(p=>`Фото: ${p.caption}`), ...state.videos.map(v=>`Відео: ${v.caption}`)].slice(0,6).join(' | '); if(generationMode==='rework' && sourcePost){document.getElementById('generatedText').value=`Режим: перегенерація з іншого поста
Дата публікації: ${humanDate(state.selectedDate)}
Тема: ${topic}
Рубрика: ${rubric}
Плейсмент: ${placement}
Формат: ${format}
Базовий пост: ${sourcePost}
Контекст із медіа: ${mediaContext||'немає'}

Перегенерований варіант під ${placement}:
${sourcePost}

Адаптація:
- скорочуємо або розширюємо під формат ${format}
- додаємо релевантний CTA
- змінюємо тон під ${placement}
- враховуємо рубрику “${rubric}”`; } else {document.getElementById('generatedText').value=`Режим: генерація з нуля
Дата публікації: ${humanDate(state.selectedDate)}
Тема: ${topic}
Рубрика: ${rubric}
Плейсмент: ${placement}
Формат: ${format}
Рекомендований візуал: ${size}
Святковий контекст: ${holiday}
Контекст із нарад: ${meetingsContext||'немає'}
Контекст із медіа: ${mediaContext||'немає'}

Гачок: ${topic} — тема, яка має зачепити аудиторію саме в ${placement}.

Основна думка: У рубриці “${rubric}” важливо подати сенс через формат “${format}”, враховуючи контекст бізнесу, нарад і пов'язаного медіаконтенту.

Структура:
1. Хук
2. Основна користь
3. Приклад / кейс / пояснення
4. CTA`; } const post=currentDraft(); post.text=document.getElementById('generatedText').value; post.status='gen'; state.posts[state.selectedDate]=[post]; saveState(); fillEditor(); drawCalendar();}); document.getElementById('generateImage').addEventListener('click',()=>{const topic=document.getElementById('customTopic').value||document.getElementById('topicPreset').value; document.getElementById('imageTitle').textContent=topic; document.getElementById('imageDesc').textContent=`Плейсмент: ${document.getElementById('placementSelect').value} • Формат: ${document.getElementById('formatSelect').value}`; const post=currentDraft(); post.text=document.getElementById('generatedText').value; post.status='gen'; state.posts[state.selectedDate]=[post]; saveState(); fillEditor(); drawCalendar();}); document.getElementById('visualUpload').addEventListener('change',e=>{const file=e.target.files[0]; if(!file)return; const reader=new FileReader(); reader.onload=()=>{const post=currentDraft(); post.image=reader.result; post.status='draft'; state.posts[state.selectedDate]=[post]; saveState(); fillEditor(); drawCalendar();}; reader.readAsDataURL(file)}); enableEnterSubmit('postForm',()=>savePost('draft')); document.getElementById('approvePost').addEventListener('click',()=>savePost('done')); drawFilters(); drawCalendar(); fillEditor();}
function renderSettingsPage(){renderSidebar('settings'); renderHeader('Константи','Aima • рубрики / плейсменти / формати'); bindCollection('rubrics','rubricsList','rubricInput','addRubric'); bindCollection('placements','placementsList','placementInput','addPlacement'); bindCollection('formats','formatsList','formatInput','addFormat'); enableEnterSubmit('rubricForm',()=>document.getElementById('addRubric').click()); enableEnterSubmit('placementForm',()=>document.getElementById('addPlacement').click()); enableEnterSubmit('formatForm',()=>document.getElementById('addFormat').click());}
function renderTopicsPage(){renderSidebar('topics'); renderHeader('Теми','Aima • окрема вкладка тем'); const stats=document.getElementById('topicsStats'); stats.innerHTML=`<div class="stat-card"><strong>${state.topics.length}</strong><span>Тем</span></div><div class="stat-card"><strong>${state.meetings.length}</strong><span>Нарад</span></div><div class="stat-card"><strong>${countAllPosts()}</strong><span>Постів</span></div><div class="stat-card"><strong>5</strong><span>За генерацію</span></div>`; bindCollection('topics','topicsList','topicInput','addTopic'); enableEnterSubmit('topicForm',()=>document.getElementById('addTopic').click()); document.getElementById('generateTopicsFromMeetings').addEventListener('click',()=>{const generated=generateTopicsFromMeetings(); const preview=document.getElementById('generatedTopicsPreview'); preview.innerHTML=generated.map((topic,idx)=>`<div class="generated-preview-item"><strong>${topic}</strong><div class="top-actions"><button class="btn btn-secondary" type="button" data-push-topic="${idx}">Додати</button></div></div>`).join(''); preview.querySelectorAll('[data-push-topic]').forEach(btn=>btn.addEventListener('click',()=>{const topic=generated[Number(btn.dataset.pushTopic)]; if(!state.topics.includes(topic)) state.topics.unshift(topic); saveState(); renderTopicsPage();}))})}
function renderMeetingsPage(){renderSidebar('meetings'); renderHeader('Наради','Aima • контекст для генерації тем'); const renderMeetings=()=>{const list=document.getElementById('meetingsList'); list.innerHTML=state.meetings.map((item,idx)=>`<div class="setting-row"><span><strong>${item.title}</strong><br><small>${item.date} — ${item.notes}</small></span><div class="row-actions"><button class="mini-btn danger" data-remove-meeting="${idx}">Вид.</button></div></div>`).join(''); list.querySelectorAll('[data-remove-meeting]').forEach(btn=>btn.addEventListener('click',()=>{state.meetings.splice(Number(btn.dataset.removeMeeting),1); saveState(); renderMeetingsPage();}))}; renderMeetings(); enableEnterSubmit('meetingForm',()=>{const title=document.getElementById('meetingTitle').value.trim(), date=document.getElementById('meetingDate').value||new Date().toISOString().slice(0,10), notes=document.getElementById('meetingNotes').value.trim(); if(!title||!notes)return; state.meetings.unshift({title,date,notes}); saveState(); document.getElementById('meetingTitle').value=''; document.getElementById('meetingDate').value=''; document.getElementById('meetingNotes').value=''; renderMeetingsPage();})}
window.addEventListener('DOMContentLoaded',()=>{loadState(); const page=document.body.dataset.page; if(page==='calendar')renderCalendarPage(); if(page==='settings')renderSettingsPage(); if(page==='topics')renderTopicsPage(); if(page==='meetings')renderMeetingsPage();});s="mini-btn danger" data-remove-video="${idx}">Вид.</button></div></div>`).join(''); list.querySelectorAll('[data-remove-video]').forEach(btn=>btn.addEventListener('click',()=>{state.videos.splice(Number(btn.dataset.removeVideo),1); saveState(); renderVideosPage();}))}; render(); enableEnterSubmit('videoForm',()=>{const file=document.getElementById('videoUpload').files[0]; const caption=document.getElementById('videoCaption').value.trim(); if(!file||!caption)return; state.videos.unshift({caption,name:file.name}); saveState(); document.getElementById('videoUpload').value=''; document.getElementById('videoCaption').value=''; renderVideosPage();});}
window.addEventListener('DOMContentLoaded',()=>{loadState(); const page=document.body.dataset.page; if(page==='calendar')renderCalendarPage(); if(page==='settings')renderSettingsPage(); if(page==='topics')renderTopicsPage(); if(page==='meetings')renderMeetingsPage(); if(page==='photos')renderPhotosPage(); if(page==='videos')renderVideosPage();});=='videos')renderVideosPage();});